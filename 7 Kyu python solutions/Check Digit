[Codewars challenge Check Digit](https://www.codewars.com/kata/5a2e8c0955519e54bf0000bd)

def check_digit(number, index1, index2, digit):
    if index1 < index2:
        return str(number)[index1:index2 + 1].find(str(digit)) != -1
    else:
        return str(number)[index2:index1 + 1].find(str(digit)) != -1
